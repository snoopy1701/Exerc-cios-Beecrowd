#include <stdio.h>
 
int main() {
 
 int tempo, velocidade;
 double dist, litros;
 
 scanf("%d %d", &tempo, &velocidade);
 dist = tempo*velocidade;
 litros = dist/12;
 printf("%.3lf\n", litros);
 
    return 0;
}
